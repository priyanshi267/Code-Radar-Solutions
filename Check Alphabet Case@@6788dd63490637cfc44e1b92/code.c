#include <stdio.h>

int main() {
    char c;
    scanf("%c",&c);
    if(n>='A' && n<='Z') {
        printf("Uppercase");
    }
    else {
        printf("Lowercase");
    }
    return 0;
}